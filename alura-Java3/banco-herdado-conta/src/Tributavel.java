//INTERFACE não pode possuir atributos ou métodos concretos (com implementação)
public interface Tributavel {

	public abstract double getValorImposto();//n precisa nem escrever que o método é abstrato pois já é padrão para a interface
}
